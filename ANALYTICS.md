# Google Analytics 리포트 보는 법

측정 ID: `G-R4772MLH47`

## 1. 이벤트 이름만 봐도 답이 나온다

만족도 답변은 파라미터가 아니라 **이벤트 이름**으로 갈라 보냅니다.
그래서 아무 설정 없이 **보고서 → 참여도 → 이벤트** 의 `이벤트 이름별 이벤트 수`
목록에서 바로 읽힙니다.

| 이벤트 이름 | 뜻 |
| --- | --- |
| `feedback_found` | "원하는 식당을 찾으셨나요?" 에 **네** 라고 답한 수 |
| `feedback_not_found` | 같은 질문에 **아니요** 라고 답한 수 |
| `search_no_result` | 필터 결과가 **0곳**이었던 검색 |
| `restaurant_search` | 필터로 검색한 전체 횟수 |
| `random_pick` | 랜덤 추천을 뽑은 횟수 |

만족도 = `feedback_found` ÷ (`feedback_found` + `feedback_not_found`)

`search_no_result` 는 버튼을 안 눌러도 잡히는 신호라 응답 수가 적을 때 특히 쓸모 있습니다.
`restaurant_search` 대비 비율이 곧 **헛검색 비율**입니다.

## 2. 파라미터를 리포트에 띄우려면 (맞춤 정의 등록)

GA4 는 **직접 만든 파라미터를 등록하기 전에는 표준 리포트에 보여주지 않습니다.**
(실시간·DebugView 에서는 등록 없이도 보입니다.)

**관리 → 데이터 표시 → 맞춤 정의 → 맞춤 측정기준 만들기** 에서 아래를 등록하세요.
범위는 전부 **이벤트** 입니다. 등록 후 **24시간쯤 지나야** 데이터가 쌓이기 시작합니다.

| 측정기준 이름 | 이벤트 매개변수 | 무엇을 보나 |
| --- | --- | --- |
| 결과 개수 구간 | `result_bucket` | `결과 0곳 (못 찾음)` / `1~3곳` / `4~9곳` / `10곳 이상` |
| 답변 | `found_label` | `찾았어요` / `못 찾았어요` |
| 캠퍼스 | `campus` | 인문/자연, 없으면 `(전체)` |
| 교내외 | `area` | 교내/교외 |
| 음식 종류 | `dish` | 한식, 돈까스 … |
| 인원 | `serving` | 혼밥/나눠먹기 |
| 가격대 | `price` | A/B/C |
| 영업 필터 | `open_now` | `영업 중만` / `전체` |

> 숫자 그대로인 `result_count` 도 함께 보내지만, 리포트에서는
> **값이 `1`, `7` 처럼 맨숫자로만 찍혀 뜻을 알기 어렵습니다.**
> 그래서 같은 내용을 한글 구간 라벨(`result_bucket`)로 한 번 더 보냅니다.
> 등록은 `result_bucket` 쪽을 하세요.

## 3. "왜 못 찾았나" 를 보려면

**탐색 → 자유 형식** 에서

- 행: `음식 종류`(`dish`) 또는 `캠퍼스`(`campus`)
- 열: `이벤트 이름`
- 값: `이벤트 수`

로 놓으면 `feedback_not_found` / `search_no_result` 가 **어떤 필터 조합에서**
많이 터지는지 나옵니다. 그 조합이 곧 식당을 더 채워야 할 칸입니다.

## 4. Supabase 로그와의 관계

같은 사건을 Supabase `search_logs` 에도 넣습니다 ([supabase/analytics.sql](./supabase/analytics.sql)).
GA 는 추세를 빨리 보는 용도, Supabase 는 `result_ids` 까지 들어 있어
SQL 로 깊게 파는 용도입니다. GA 쪽 이벤트 이름을 나눈 것은 리포트 가독성 때문이고,
Supabase 의 `kind` 값(`search` / `pick` / `feedback`)은 그대로입니다.
