# 구글 검색에 뜨게 하기

목표 검색어: **성균관대 맛집 찾기**

코드 쪽 세팅은 끝났습니다. 아래 3단계는 **웹사이트에서 직접 눌러야 하는 일**이라
코드로 대신할 수 없습니다. 순서대로 하세요.

## 1단계 · 배포부터 (이게 없으면 나머지가 전부 무의미)

지금 이 서비스는 인터넷에 없습니다. 검색엔진이 읽을 주소가 없으니 당연히 검색도 안 됩니다.

1. GitHub 저장소 → **Settings** → 왼쪽 메뉴 **Pages**
2. **Source** 를 `Deploy from a branch` 로
3. **Branch** 를 `main` / `/ (root)` 로 두고 **Save**
4. 1~3분 뒤 아래 주소가 열리는지 확인

```
https://cms9909-a11y.github.io/food_service/
```

> 저장소 이름이 `food_service` 입니다. 로컬 폴더 이름(`food_survice`)과 철자가 다르니
> 주소는 저장소 쪽(`food_service`)을 따릅니다.

## 2단계 · 구글에 "여기 있습니다" 라고 알리기

가만히 두면 구글이 알아서 찾아오긴 하지만 몇 주가 걸립니다. Search Console 로 알리면 며칠로 줄어듭니다.

1. [Google Search Console](https://search.google.com/search-console) 접속 (GA 쓰던 계정 그대로)
2. **속성 추가** → 왼쪽 **URL 접두어** 를 고르고 위 주소를 그대로 입력
   - `github.io` 는 남의 도메인이라 **도메인** 방식은 못 씁니다. 반드시 URL 접두어입니다.
3. 소유권 확인 → **HTML 파일** 방식을 고르고, 받은 `google....html` 파일을
   **저장소 최상단(index.html 옆)** 에 넣고 커밋 → 푸시 → **확인** 클릭
4. 확인되면 왼쪽 **Sitemaps** → `sitemap.xml` 입력 → 제출
5. 맨 위 **URL 검사** 칸에 위 주소를 붙여넣고 → **색인 생성 요청**

여기까지 하면 보통 **2~7일** 안에 검색에 나오기 시작합니다.

## 3단계 · 확인

색인이 됐는지는 구글에 이렇게 검색해 보면 바로 압니다.

```
site:cms9909-a11y.github.io/food_service
```

결과가 나오면 색인 완료입니다. 그때부터 "성균관대 맛집 찾기" 로 검색해 보세요.

---

## 코드에 넣어둔 것

| 무엇 | 어디 | 왜 |
|---|---|---|
| `<title>` | 이미 있었음 | 검색어와 정확히 일치. 가장 강한 신호 |
| `<meta name="description">` | index.html `<head>` | 검색결과에 뜨는 두 줄 설명 |
| `rel="canonical"` | 〃 | 필터를 걸면 `?campus=...` 가 붙는데, 전부 한 페이지로 합쳐 점수를 몰아줍니다 |
| Open Graph | 〃 | 카카오톡으로 공유했을 때 제목·설명이 뜹니다 |
| JSON-LD (`WebSite`) | 〃 | 구글이 "무엇을 하는 사이트인가" 를 구조화해 읽습니다 |
| 파비콘 | 〃 | 검색결과 제목 옆 아이콘. 외부 파일 없이 이모지를 SVG 로 넣었습니다 |
| `sitemap.xml` | 최상단 | 2단계에서 제출할 파일 |
| `robots.txt` | 최상단 | 아래 주의사항 참고 |

`<h1>` 도 `성대생을 위한 오늘의 한 끼` → **`성균관대 맛집, 오늘의 한 끼`** 로 바꿨습니다.
제목 다음으로 강한 신호라 검색어를 넣었고, 빠진 "성대생을 위한" 은 바로 아래 문장으로 옮겼습니다.
글자 수가 비슷해 디자인은 그대로입니다. 마음에 안 들면 그 두 줄만 되돌리면 됩니다.

### robots.txt 주의

검색엔진은 `robots.txt` 를 **도메인 최상위에서만** 찾습니다.

- 읽는 곳: `https://cms9909-a11y.github.io/robots.txt` ← 이 저장소 소유가 아님
- 우리 파일: `https://cms9909-a11y.github.io/food_service/robots.txt`

그래서 지금은 사실상 안 읽힙니다. 다만 **robots.txt 가 없으면 "전부 허용" 이 기본**이라
크롤링에는 아무 문제가 없고, sitemap 은 2단계에서 주소로 직접 제출하므로 괜찮습니다.
나중에 개인 도메인을 붙이면 이 파일이 그대로 제 역할을 합니다.

## 주소를 옮기게 되면

개인 도메인을 사거나 Vercel 등으로 옮기면 아래를 **전부** 새 주소로 고쳐야 합니다.
안 고치면 `canonical` 이 옛 주소를 가리켜 새 주소가 검색에서 사라집니다.

- `index.html` 의 `<!-- CANONICAL -->` 주석이 달린 2곳 + JSON-LD 의 `url`
- `sitemap.xml` 의 `<loc>`
- `robots.txt` 의 `Sitemap:`
- Search Console 에 새 속성으로 다시 등록

## 솔직한 기대치

- **"성균관대 맛집 찾기"** — 이 문장 그대로 검색하는 사람은 많지 않아 경쟁이 약합니다.
  제목·h1·설명이 정확히 일치하니 색인만 되면 상위에 붙을 가능성이 높습니다.
- **"성균관대 맛집"** — 네이버 블로그, 식당 리뷰 사이트들과 경쟁이라 훨씬 어렵습니다.
  메타 태그만으로는 부족하고, 실제로 방문자가 쌓이고 링크가 걸려야 올라갑니다.
- 한국에서 맛집은 **네이버 검색 비중이 큽니다.** 구글만으로 부족하다 싶으면
  [네이버 서치어드바이저](https://searchadvisor.naver.com) 에도 같은 방식으로 등록하세요.
  위에 넣어둔 메타 태그와 sitemap 을 그대로 씁니다.

## 남은 것 하나

**공유용 이미지(og:image)** 는 넣지 않았습니다. 카카오톡으로 링크를 보내면 지금은
글자만 뜨고 썸네일이 없습니다. 이미지는 실제 PNG 파일이 있어야 하는데
(카카오·페이스북은 SVG 를 안 받습니다) 제가 만들 수단이 없어 비워뒀습니다.

1200×630 PNG 를 하나 만들어 저장소에 넣어주시면 태그는 제가 연결하겠습니다.
