# 구글 검색에 뜨게 하기

목표 검색어: **성균관대 맛집 찾기**

서비스 주소: **https://food-service-jade.vercel.app/** (Vercel, `main` 브랜치 자동 배포)

배포와 코드 세팅은 끝났습니다. 남은 건 **Search Console 에서 직접 눌러야 하는 일**뿐입니다.

## 1단계 · Search Console 에 등록

가만히 두면 구글이 알아서 찾아오긴 하지만 몇 주가 걸립니다. 알리면 며칠로 줄어듭니다.

1. [Google Search Console](https://search.google.com/search-console) 접속 (GA 쓰던 계정 그대로)
2. **속성 추가** → 왼쪽 **URL 접두어** 를 고르고 아래를 그대로 입력

   ```
   https://food-service-jade.vercel.app/
   ```

   > `vercel.app` 은 우리 소유 도메인이 아니라 DNS 를 못 건드립니다.
   > 오른쪽 **도메인** 방식은 안 되고, 반드시 왼쪽 **URL 접두어** 여야 합니다.

3. 소유권 확인 → **HTML 태그** 방식을 고르세요.
   `<meta name="google-site-verification" content="...">` 한 줄을 줍니다.
   그 줄을 알려주시면 제가 `index.html` 의 `<head>` 에 넣고 푸시하겠습니다.
   Vercel 이 1~2분 안에 자동 배포하면 **확인** 을 누르면 됩니다.

   > **HTML 파일** 방식도 됩니다. 받은 `google....html` 을 저장소 최상단
   > (`index.html` 옆) 에 넣고 푸시하면 `.../google....html` 로 열립니다.
   > 태그 방식이 파일 하나 덜 만들어 더 깔끔합니다.

## 2단계 · 사이트맵 제출

확인이 끝나면 왼쪽 메뉴 **Sitemaps** 로 갑니다.
"새 사이트맵 추가" 칸에는 **전체 주소가 아니라 뒷부분만** 넣습니다.
앞의 `https://food-service-jade.vercel.app/` 은 이미 붙어 있습니다.

```
sitemap.xml
```

제출 직후 상태가 "가져올 수 없음" 이면 잠시 뒤 새로고침하세요.

## 3단계 · 색인 요청과 확인

맨 위 **URL 검사** 칸에 서비스 주소를 붙여넣고 → **색인 생성 요청**.

며칠 뒤 구글에 이렇게 검색해 확인합니다.

```
site:food-service-jade.vercel.app
```

결과가 나오면 색인 완료입니다. 그때부터 "성균관대 맛집 찾기" 로 검색해 보세요.
보통 **2~7일** 걸립니다.

---

## 코드에 넣어둔 것

| 무엇 | 어디 | 왜 |
|---|---|---|
| `<title>` | 이미 있었음 | 검색어와 정확히 일치. 가장 강한 신호 |
| `<meta name="description">` | index.html `<head>` | 검색결과에 뜨는 두 줄 설명 |
| `rel="canonical"` | 〃 | 필터를 걸면 `?campus=...` 가 붙는데, 전부 한 페이지로 합쳐 점수를 몰아줍니다 |
| Open Graph | 〃 | 카카오톡으로 공유했을 때 제목·설명이 뜹니다 |
| JSON-LD (`WebSite`) | 〃 | 구글이 "무엇을 하는 사이트인가" 를 구조화해 읽습니다 |
| 파비콘 | 〃 | 검색결과 제목 옆 아이콘. 외부 파일 없이 이모지를 SVG 로 넣었습니다 |
| `sitemap.xml` | 최상단 | 2단계에서 제출할 파일 |
| `robots.txt` | 최상단 | 전부 허용 + sitemap 위치 안내 |

`<h1>` 도 `성대생을 위한 오늘의 한 끼` → **`성균관대 맛집, 오늘의 한 끼`** 로 바꿨습니다.
제목 다음으로 강한 신호라 검색어를 넣었고, 빠진 "성대생을 위한" 은 바로 아래 문장으로 옮겼습니다.
글자 수가 비슷해 디자인은 그대로입니다. 마음에 안 들면 그 두 줄만 되돌리면 됩니다.

## 주소를 옮기게 되면

개인 도메인을 사면 아래를 **전부** 새 주소로 고쳐야 합니다.
안 고치면 `canonical` 이 옛 주소를 가리켜 새 주소가 검색에서 사라집니다.
(실제로 한 번 겪었습니다 — github.io 주소로 잘못 적혀 있었습니다.)

- `index.html` 의 `<!-- CANONICAL -->` 주석이 달린 2곳 + JSON-LD 의 `url`
- `sitemap.xml` 의 `<loc>`
- `robots.txt` 의 `Sitemap:`
- Search Console 에 새 속성으로 다시 등록

한 번에 바꾸려면:

```bash
sed -i 's#https://food-service-jade\.vercel\.app/#새주소#g' \
  index.html sitemap.xml robots.txt SEO.md
```

## 솔직한 기대치

- **"성균관대 맛집 찾기"** — 이 문장 그대로 검색하는 사람은 많지 않아 경쟁이 약합니다.
  제목·h1·설명이 정확히 일치하니 색인만 되면 상위에 붙을 가능성이 높습니다.
- **"성균관대 맛집"** — 네이버 블로그, 식당 리뷰 사이트들과 경쟁이라 훨씬 어렵습니다.
  메타 태그만으로는 부족하고, 실제로 방문자가 쌓이고 링크가 걸려야 올라갑니다.
- `vercel.app` 같은 무료 하위 도메인은 개인 도메인보다 불리합니다.
  본격적으로 노출을 노린다면 도메인 하나 사는 게 가장 효과가 큽니다.
- 한국에서 맛집은 **네이버 검색 비중이 큽니다.** 구글만으로 부족하다 싶으면
  [네이버 서치어드바이저](https://searchadvisor.naver.com) 에도 같은 방식으로 등록하세요.
  위에 넣어둔 메타 태그와 sitemap 을 그대로 씁니다.

## 남은 것 하나

**공유용 이미지(og:image)** 는 넣지 않았습니다. 카카오톡으로 링크를 보내면 지금은
글자만 뜨고 썸네일이 없습니다. 이미지는 실제 PNG 파일이 있어야 하는데
(카카오·페이스북은 SVG 를 안 받습니다) 제가 만들 수단이 없어 비워뒀습니다.

1200×630 PNG 를 하나 만들어 저장소에 넣어주시면 태그는 제가 연결하겠습니다.
